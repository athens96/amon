# A-mon 서버 송신 프로토콜

## 상태

기존 서버 보고 프로토콜과 macOS/Windows 송신 구현은 제거되었다.

제거된 엔드포인트:

- `POST /api/v1/ai-live/report`
- `POST /api/v1/ai-live/history`
- `POST /api/v1/ai-agents/report`

A-mon은 현재 사용량, 라이브 세션, 종료 세션, SQLite 스냅샷을 자체 서버로 보내지 않는다. `server_url`은 앱 업데이트 확인과 다운로드에만 사용한다. 각 AI 공급자의 사용량을 조회하기 위한 외부 API 요청은 이 자체 서버 송신과 별개이므로 유지한다.

## 다음 프로토콜 요구사항

새 송신 프로토콜은 다음 원칙으로 설계한다.

1. 데이터는 사용자별로 분리해 전송하고 집계한다.
2. 송신 단위는 사용자가 지정한 "패킷의 양"을 기준으로 한다.
3. 사용자 식별자, 패킷의 정의와 단위, 배치 크기, 전송 주기, 중복 방지 키가 확정되기 전에는 클라이언트 송신을 구현하지 않는다.
4. 사용자 식별 정보와 인증 정보는 로그에 기록하지 않는다.
5. 서버는 사용자 간 데이터 혼합을 방지하고 중복 요청을 안전하게 처리해야 한다.

## 미확정 계약

아래 항목이 정해지면 이 문서를 구체적인 wire protocol로 갱신한다.

| 항목 | 결정 필요 내용 |
|---|---|
| 사용자 식별 | 내부 사용자 ID, API key, JWT 등 |
| 패킷의 정의 | 네트워크 패킷 수, 이벤트 수, 토큰/사용량 레코드 수 등 |
| 배치 조건 | N개 도달, 시간 간격, 둘 중 먼저 충족 등 |
| 요청 형식 | endpoint, headers, JSON schema |
| 전달 보장 | at-most-once, at-least-once, exactly-once에 준하는 멱등 처리 |
| 실패 처리 | 재시도 횟수, backoff, 로컬 큐 보존 기간 |
| 개인정보 | 수집 허용 필드와 보존 기간 |

## 구현 금지선

새 계약이 확정되기 전까지 다음 항목을 다시 추가하지 않는다.

- 라이브 세션·프롬프트·응답 요약 송신
- 종료 세션 기록 송신
- 로컬 `usage.db` 파일 업로드
- 사용자 구분 없는 공용 집계 요청
